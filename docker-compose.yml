services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
    environment:
      - DEBUG=True
      - SECRET_KEY=your-secret-key-here
    restart: unless-stopped

volumes:
  static_volume:
